<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MovieIntroduction" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">#93FFA5</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:textViewStyle">@style/jejuTextView</item>
        <item name="android:buttonStyle">@style/jejuButton</item>

        <!-- color highlight -->
        <item name="colorControlHighlight">#fd4381</item>
    </style>
    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/endgameposter</item>
    </style>
    <style name="jejuTextView" parent="android:Widget.TextView">
        <item name="android:fontFamily">@font/jejuhallasan</item>
    </style>
    <style name="jejuButton" parent="android:Widget.Button">
        <item name="android:fontFamily">@font/jejuhallasan</item>
    </style>
</resources>